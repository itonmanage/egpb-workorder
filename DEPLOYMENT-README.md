# 🚀 Standalone Deployment - Quick Start

ไฟล์และเอกสารสำหรับ deploy Next.js application แบบ standalone บน Nutanix VM

## 📁 ไฟล์ที่สร้างขึ้น

| ไฟล์ | คำอธิบาย |
|------|----------|
| `ecosystem.config.js` | PM2 configuration สำหรับจัดการ process |
| `env.production.example` | Template สำหรับ environment variables |
| `deploy.sh` | Script สำหรับ deploy/update (zero-downtime) |
| `rollback.sh` | Script สำหรับ rollback ไปยัง version ก่อนหน้า |
| `DEPLOYMENT.md` | คู่มือการ deploy แบบละเอียด (ภาษาไทย) |

## ⚡ Quick Start

### 1. ตั้งค่าครั้งแรก

```bash
# คัดลอก environment template
cp env.production.example .env.production

# แก้ไข environment variables
nano .env.production

# ทำให้ scripts รันได้
chmod +x deploy.sh rollback.sh
```

### 2. Deploy

```bash
# Deploy ครั้งแรก หรือ update
./deploy.sh
```

### 3. Rollback (ถ้าจำเป็น)

```bash
# Rollback ไปยัง version ก่อนหน้า
./rollback.sh
```

## 📖 เอกสารเพิ่มเติม

อ่านคู่มือฉบับเต็มได้ที่: **[DEPLOYMENT.md](./DEPLOYMENT.md)**

ครอบคลุม:
- ✅ สิ่งที่ต้องเตรียม
- ✅ การติดตั้งครั้งแรก (ทีละขั้นตอน)
- ✅ การ Deploy/Update
- ✅ การ Rollback
- ✅ การตรวจสอบและ Monitoring
- ✅ การแก้ไขปัญหา
- ✅ คำสั่งที่เป็นประโยชน์
- ✅ Security Best Practices
- ✅ Performance Tuning

## 🎯 สิ่งที่ Deployment Script ทำให้

### `deploy.sh`
- ✅ ตรวจสอบ prerequisites (Node.js, npm, PM2)
- ✅ ตรวจสอบ database migration
- ✅ Backup version เก่าอัตโนมัติ
- ✅ Build Next.js standalone
- ✅ Copy static files และ public folder
- ✅ อัพเดท PM2 configuration
- ✅ Reload PM2 แบบ zero-downtime
- ✅ ตรวจสอบว่า application รันสำเร็จ
- ✅ Rollback อัตโนมัติถ้ามีปัญหา
- ✅ ทำความสะอาด backup เก่า (เก็บไว้ 5 versions)

### `rollback.sh`
- ✅ แสดงรายการ backup ที่มี
- ✅ ให้เลือก version ที่ต้องการ rollback
- ✅ แจ้งเตือนถ้ามี database migration
- ✅ Backup version ปัจจุบันก่อน rollback
- ✅ Rollback และ reload PM2
- ✅ ตรวจสอบว่า application รันสำเร็จ

## 🔧 คำสั่งที่ใช้บ่อย

```bash
# ดู PM2 status
pm2 status

# ดู logs
pm2 logs ticket-app

# Restart application
pm2 restart ticket-app

# Monitoring
pm2 monit
```

## ⚠️ สิ่งสำคัญที่ต้องจำ

1. **Environment Variables**
   - แก้ไข `.env.production` ให้ตรงกับ server จริง
   - เปลี่ยน `JWT_SECRET` เป็นค่าที่ปลอดภัย
   - ไม่ commit `.env.production` เข้า git

2. **Database**
   - Rollback จะเปลี่ยนเฉพาะ code เท่านั้น
   - Database จะไม่ถูกแตะต้อง
   - ถ้ามี migration ใหม่ ต้องระวังเวลา rollback

3. **Backup**
   - Deployment script จะ backup อัตโนมัติ
   - Backup เก็บไว้ใน `backups/` directory
   - เก็บไว้ล่าสุด 5 versions

## 🆘 เจอปัญหา?

อ่านส่วน **[การแก้ไขปัญหา](./DEPLOYMENT.md#การแก้ไขปัญหา)** ใน DEPLOYMENT.md

---

**หมายเหตุ:** ไฟล์เหล่านี้สร้างขึ้นสำหรับ deploy บน Nutanix VM (Linux) โดยเฉพาะ
